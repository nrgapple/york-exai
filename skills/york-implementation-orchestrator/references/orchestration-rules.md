# Orchestration Rules

- Use Codex for software build work once a packet is ready.
- Keep product clarification, workflow definition, decision rules, and acceptance criteria in docs before code starts.
- If a packet leaves meaningful decisions unresolved, send it back to PD&E instead of pushing uncertainty into implementation.
- Route bugs and regressions back through Product Planning after build.
