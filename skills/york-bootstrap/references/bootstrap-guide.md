# Bootstrap Guide

- Confirm the surrounding workspace understands this is a pest-control operation in central Pennsylvania.
- Make sure the workspace memory picks up the facts in `MEMORY-SEEDS.md`.
- Ensure human-role logic follows `context/human-roles.md`.
- Ensure Joel-facing voice follows `context/voice-and-tone.md`.
- Install or link the skills in `skills/`.
- Review `integrations/` to mark what is configured, blocked, or missing.
- Queue build work from `backlog/roadmap.md` and `backlog/work-packets/`.
